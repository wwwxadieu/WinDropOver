<Window x:Class="WinClipboard.App.Views.BubbleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WinClipboard"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True"
        ResizeMode="NoResize"
        Width="72" Height="72"
        AllowDrop="True"
        SourceInitialized="OnSourceInitialized"
        DragEnter="OnDragEnter"
        DragLeave="OnDragLeave"
        Drop="OnDrop"
        MouseLeftButtonUp="OnClicked">
    <Grid>
        <Border x:Name="BubbleBorder" CornerRadius="36" Background="{StaticResource AccentBrush}" Opacity="0.92">
            <Border.Effect>
                <DropShadowEffect Color="Black" Opacity="0.4" BlurRadius="18" ShadowDepth="2" />
            </Border.Effect>
        </Border>
        <TextBlock x:Name="CountText" Text="0" Foreground="White" FontSize="22" FontWeight="Bold"
                   HorizontalAlignment="Center" VerticalAlignment="Center" IsHitTestVisible="False" />
    </Grid>
</Window>
